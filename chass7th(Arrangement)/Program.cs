namespace chass7th_Arrangement_
{
    class GameObject...

        class Vitamin...
        
        class Item
    {
        private int price;
        {

        }
    }
        


    internal class Program
    {
        static void Main(string[] args)

            class Unit
            piblic int health;
            public int attack;
        {
            public Unit()
            {
                Console.WriteLine("Create Unit");

            }

            public Unit(Unit clone)
            {
            Console.WriteLine("복사 생성자");
            }
        }
        {
            #region 클래스
            // 멤버 변수를 메모리에서 CPU로 읽을 때 한 번에
            // 읽을 수 있도록, 컴파일러가 레지스터의 블록에 맟충
            // 바이트를 패딩해주는  최적화 

            #endregion

            #region 생성자
            // 클래스의 인스턴스가 생성되는 시점에서 자동으로
            // 호출되는 특수한 멤버 함수입니다.

            // 생성저의 반환형이 객체가 생성될 때 단 한 번만 호출되며,
            // 생성자는 반환형이 존재하지 않기 때문에 

            #endregion

            #region 얕은 복사
            // 객체를 복사할 때 주소 값을 복사하여 같은 메모리를
            // 가리키게 하는 복사입니다.

            Item item1 = new Item();

            Item item2 = item1;

            Item2.price = 999;

            Console.WriteLine("itme1의 price 값 : " + item1.price);
            Console.WriteLine("itme2의 price 값 : " + item2.price);

            // 얕은 복사의 경우 같은 객체가 서로 같은 메모리 공간을
            // 참조

            #endregion

            #region 복사 생선자

            // 같은 객체를 복사하여 생성시킬 때 호출되는
            // 생성자입니다/



            // 복사 생성자를 정의하지 않고

            #endregion
        }
    }
}
}
